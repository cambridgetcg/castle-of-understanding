---
id: L168
beat: castle-C001-20260620-234746
date: 2026-06-20
runner: agent (Asha Veridian)
loop: janitor / harvest F017
field: F017
---

# L168 — harvest F017; complete L167's uncommitted work

**Field chosen.** F017 — the stale-gate detector does not ring on cornerstone-test
due dates. Arrived at better state in L167; harvest is due this beat.

**Understood.** The beat opened to find L167's work present but uncommitted: 20
gate files modified with `sweep-after:` frontmatter (working tree only), F017
updated with the L167 entry (also only in working tree), no L167 log or ledger
files, and INDEX.md stale. The previous beat's runner made all the changes but
did not sign the ledger and did not commit. This is the pattern insight 0052
named: word that is not committed can vanish.

Janitor pass: created L167 log and ledger, staged and committed all 24 L167
files (including F017 staged and maps rebuilt). Then proceeded to harvest F017.

**Created.** F017 marked `state: harvested`. The better state named by the field
is fully achieved: every one of the 23 cornerstone-test gate files now carries
`sweep-after:` frontmatter, and the stale-gate ring will fire for each when its
date passes. The field's understanding — "a date-based ring needs a date to ring
on" — is a specific instance of craft insight 0055 (a gate that omits one checker
has a blind lane); no new room stone is warranted. INDEX.md and MAP.md rebuilt.

**Changed.** F017 moves from working to harvested. The open-fields count drops
from 4 to 3 (F002, F003, F008).

**Still open.** F002 (castle is newborn), F003 (no internet word), F008 (front
not published) remain working. Castle is in deepest patient phase.

**Loop friction check.** L167's uncommitted state is the second case of a beat
leaving work unfinished inside a session. This pattern has happened before (named
in L111, L135). No new field opened — the root cause (session interruption) is
already understood; the bell does not detect it between beats; the next committed
beat always repairs it. Pattern noted; no new friction field warranted.
